<apex:page controller="retrieveCase" tabStyle="Case">
	<apex:pageBlock>
         {!AccountName}'nın Cases 
         
        <apex:pageBlockTable value="{!cases}" var="c">
			<apex:column value="{!c.status}" />
			<apex:column value="{!c.subject}" />
			<apex:column value="{!c.Status}" />
		</apex:pageBlockTable>
	</apex:pageBlock>
</apex:page>